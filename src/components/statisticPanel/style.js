import styled from 'styled-components';

export const StatisticPanelWrapper = styled.div`
  background-color: #fff;
  .header {
    font-size: 14px;
    color: rgba(0, 0, 0, 0.45);
    display: flex;
    justify-content: space-between;
  }

  .content {
    padding: 10px 0;
    border-bottom: 1px solid #f0f0f0;
    font-size: 26px;
    color: rgba(0, 0, 0, 0.85);
    text-align: start;
  }

  .bottom {
    padding-top: 10px;
    color: rgba(0, 0, 0, 0.85);
    font-size: 14px;
    text-align: start;
  }
`