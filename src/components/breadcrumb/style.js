import styled from 'styled-components';

export const BreadcrumbWrapper = styled.div`
  display: flex;
  align-items: center;
  font-size: 30px;
  .trigger{
    cursor: pointer;
    margin-right: 10px;
  }
`