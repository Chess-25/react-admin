import React, { memo } from 'react';

import { OverviewWrapper } from "./style"

const Overview = memo(() => {
  return (
    <OverviewWrapper>
      Overview
    </OverviewWrapper>
  );
});

export default Overview;